"" IdaVim Suggestions

" use leader for the IdeaVim commands; set leader to space
let mapleader=" "

" use the system clipboard
set clipboard+=unnamed
set clipboard+=ideaput

"" -- Map IDE actions to IdeaVim -- https://jb.gg/abva4t
map <leader>x <Action>(Run)
map <leader>d <Action>(Debug)
map <leader>b <Action>(ToggleLineBreakpoint)
nmap <leader>= <Action>(ReformatCode)
map <leader>z <Action>(ToggleDistractionFreeMode)
" Actions with dots don't work with <Action>() syntax
nmap <leader>r :action Refactorings.QuickListPopupAction<CR>
" Open the extract/introduce menu
nmap <leader>e <Action>(IntroduceActionsGroup)
" Open the SurroundWith menu
nmap <leader>w <Action>(SurroundWith)
" Show usages
map <leader>u <Action>(ShowUsages)
" Find in files
map <leader>/ <Action>(FindInPath)

" Recursive code folding fixes
nnoremap zC :action CollapseRegionRecursively<CR>
nnoremap zO :action ExpandRegionRecursively<CR>

" Navigate methods with vim defaults
nmap ]m <Action>(MethodDown)
nmap [m <Action>(MethodUp)

" Goto: Class, File, Symbol
map <leader>gc <Action>(GotoClass)
map <leader>gf <Action>(GotoFile)
map <leader>gs <Action>(GotoSymbol)

" Managing splits and tabs in them
map <leader>s <Action>(MoveTabRight)
map <leader>v <Action>(MoveTabDown)
map <leader>m <Action>(MoveEditorToOppositeTabGroup)

" Find more examples here: https://jb.gg/share-ideavimrc

" == PLUGINS ==

set multicursor " ms/pattern to create cursors at all pattern matches

" == VIM ==

source ~/.vim/vimrc
